# Active Runtime Session Fixtures v0.2

This directory is reserved for runtime session smoke fixtures that load active
graph v0.2 project payloads. The current executable Runtime session smoke
coverage remains under `compatibility/v0.1/runtime-session-fixtures` as legacy
API compatibility until the released Runtime session API fully accepts the
active graph v0.2 mutation model.
