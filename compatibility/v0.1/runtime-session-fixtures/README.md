# Legacy Runtime Session Fixtures v0.1

These session smoke fixtures cover the legacy v0.1 Runtime session API and
project payload shape. They remain useful for import and compatibility checks,
but they are not the active graph v0.2 collaboration/session fixture model.

Active collaboration wire fixtures live in `compatibility/v0.2/collaboration`.
