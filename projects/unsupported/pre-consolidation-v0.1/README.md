# Unsupported Pre-Consolidation Project Fixtures

These project files use the unsupported pre-consolidation graph v0.1 example
shape. They are not the active project model.

Active product project examples live in `projects/v0.1`.
