# Active Project Examples v0.2

These project examples are active graph v0.2 fixtures. They use
`ProjectDocumentV02`, `GraphDocumentV02`, and `PatchDefinitionV02` where
subpatch or live-help behavior needs reusable graph definitions.

v0.1 project files are retained in `projects/v0.1` only as legacy import and
migration coverage.
