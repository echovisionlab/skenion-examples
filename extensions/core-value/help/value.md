# Example Value

Minimal first-party-style package node. It uses the same extension package
manifest shape as third-party packages so Runtime and Studio can load the
contract surface without special casing builtins.
