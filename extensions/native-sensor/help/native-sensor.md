# Native Sensor

Minimal native extension skeleton. Build the Rust cdylib from this package
directory, then point `SKENION_EXTENSION_PATH` at the package directory so
Runtime can validate the manifest and artifact.
