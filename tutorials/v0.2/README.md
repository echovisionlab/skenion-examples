# Active Tutorial Fixtures v0.2

These tutorials are the active product examples for graph authoring. Each entry
uses `ProjectDocumentV02`/`GraphDocumentV02`, patch libraries, and v0.2 target
paths as the source of truth.

The v0.1 tutorials remain under `tutorials/v0.1` only as legacy import and
migration coverage.
